<%= form_with(model: user_name_form, **user_name_form.form_with_options) do |form| %>
  <% if user_name_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_name_form.errors.count, "error") %> prohibited this name from being saved:</h2>

      <ul>
        <% user_name_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
